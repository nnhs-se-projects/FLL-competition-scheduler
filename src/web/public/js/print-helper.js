/**
 * Print helper functions for FLL Competition Scheduler
 */

// eslint-disable-next-line no-unused-vars
function printSchedule() {
  // Simply trigger the print dialog directly without any style modifications
  window.print();
}
