<div class="overflow-x-auto">
  <table class="min-w-full divide-y divide-gray-200 print-table">
    <colgroup>
      <col style="width: 15%" />
      <col style="width: 10%" />
      <col style="width: 35%" />
      <col style="width: 20%" />
      <col style="width: 15%" />
    </colgroup>
    <thead class="bg-gray-50">
      <tr>
        <th
          class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
        >
          Time
        </th>
        <th
          class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
        >
          Team #
        </th>
        <th
          class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
        >
          Team Name
        </th>
        <th
          class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
        >
          Activity
        </th>
        <th
          class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
        >
          Duration
        </th>
      </tr>
    </thead>
    <tbody class="bg-white divide-y divide-gray-200">
      <% table.sort((a, b) => a.startTime - b.startTime).forEach(event => { %>
      <tr class="hover:bg-gray-50 schedule-item">
        <td
          class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 font-medium"
        >
          <%= Math.floor(event.startTime/60) %>:<%=
          String(event.startTime%60).padStart(2, '0') %>
        </td>
        <td
          class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 font-medium"
        >
          <%= event.teamID || event.teamId || "-" %>
        </td>
        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
          <%= event.teamName || "N/A" %>
        </td>
        <td class="px-6 py-4 whitespace-nowrap text-sm">
          <% if (event.type === 'tableRun') { %>
          <span class="text-blue-600 font-medium">Robot Game</span>
          <% } else if (event.type === 'practiceRun') { %>
          <span class="text-teal-600 font-medium">Practice Run</span>
          <% } else if (event.type === 'tableSetup') { %>
          <span class="text-gray-600 font-medium">Table Setup</span>
          <% } else if (event.type === 'robotInspection') { %>
          <span class="text-amber-600 font-medium">Robot Inspection</span>
          <% } else if (event.type === 'break') { %>
          <span class="text-gray-500 font-medium">Break</span>
          <% } else { %>
          <span class="font-medium"><%= event.type %></span>
          <% } %>
        </td>
        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
          <%= event.duration %> min
        </td>
      </tr>
      <% }); %>
    </tbody>
  </table>
</div>
